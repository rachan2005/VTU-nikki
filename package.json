{
    "name": "vtu-nikki-root",
    "version": "3.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -rv frontend/dist ./dist"
    }
}